export type User = {
  id: string
  firstName: string
  lastName: string
  about: string
}

export type LoginData = {
  name: string
  password: string
}

export type NewsItem = {
  id: number
  title: string
  body: string
}
